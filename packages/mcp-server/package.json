{
  "name": "@scalar/mcp-server",
  "description": "Servidor MCP (Model Context Protocol) autônomo e extensível para OpenClaude e Antigravity",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:*",
    "zod": "catalog:*"
  },
  "devDependencies": {
    "@types/node": "catalog:*",
    "tsx": "catalog:*"
  }
}
